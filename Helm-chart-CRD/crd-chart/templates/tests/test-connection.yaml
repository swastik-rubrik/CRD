apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crd-chart.fullname" . }}-test-connection"
  labels:
    {{- include "crd-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crd-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
